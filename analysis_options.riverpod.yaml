# Include this from Riverpod-owned modules as the migration proceeds. Legacy
# Provider screens remain below the compatibility shell until their role module
# has moved to the new composition root.
include: analysis_options.yaml

plugins:
  riverpod_lint: ^3.1.0
