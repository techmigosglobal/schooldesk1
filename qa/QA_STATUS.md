# QA Status — 2026-09-21 follow-up

## Migration parity follow-up — 2026-09-21

The previously aligned **140 migration version IDs** remain unchanged. A new local migration, `20260921043515_reconcile_legacy_schooldesk_rpcs`, is applied to Docker-local `schooldesk-local` and is the only pending production version. `supabase db push --dry-run --skip-vault --project-ref ouvwogguttybmpgfgctc` listed only that migration; no production write or history repair was performed. The 13 historical name mismatches remain documented in the [migration alignment report](reports/2026-09-21-migration-history-alignment.md); use the [migration procedure](../docs/agents/supabase-migrations.md) before promotion.

The four production `SECURITY DEFINER` RPCs have recorded service-role calls in `pg_stat_statements`; they are preserved and hardened locally, not treated as unused. Their current callers were not found in the repository or active production Edge Function versions. All four now have `search_path=''` and service-role-only grants in the new migration. Production still has its prior definitions until the pending migration is explicitly promoted. Catalog counts and digests match for relations, constraints, indexes, policy identities, triggers, selected grants, semantic columns, and public routine signatures. Seven `staff_subjects` columns retain their accepted physical ordinal difference; local and production Storage providers remain different. Full details and captured definitions are in the [RPC reconciliation report](reports/2026-09-21-legacy-schooldesk-rpc-reconciliation.md).

The bumped Android release APK and AAB `1.0.26+38` were built and verified with the production backend configuration. Flutter analysis passed and the full suite passed **799 tests with one skip**. The APK was not installed, preserving the phone's current local-backend QA build; the AAB was not uploaded to Play. Artifact metadata and signing evidence are in the [APK report](reports/2026-09-21-android-release-apk.md) and [AAB report](reports/2026-09-21-android-release-aab.md). Device walkthrough, live role workflows, FCM, reconnect, document lifecycle, browser, and iOS gates remain open; overall release status remains **BLOCKED**.

## Prior QA and promotion record — 2026-09-20

**Build/Commit:** `HEAD 7d38ab5` plus uncommitted working-tree changes. This follow-up adds approval decision auditing, Approval Center audit-log loading, and error alert delivery.

**Date:** 2026-09-20

**Changed features:** Approval Center requester hydration by user ID, staff leave date fields (`start_date`/`end_date`), durable approval decision history, Approval Center audit-log loading, error alert delivery, questionnaire execution status, QA evidence files, analyzer/dependency-tool configuration, Android logout harness, Settings tile Material surface, and local APK handoff.

## Latest promotion update — 2026-09-20

The four reviewed migrations and the `api` Edge Function were promoted to Supabase production after the fresh Docker gates. The API is revision 277 (ACTIVE); hosted `/health` returned HTTP 200. Production catalog postconditions are recorded in [the promotion report](reports/2026-09-20-production-promotion.md). No production test identities or synthetic business records were available, so authenticated live approval, notification, and document workflows remain unverified. Migration version parity was completed on 2026-09-21; see the follow-up above.

The fresh Docker stack replayed all 140 checked-in migrations. Edge tests passed 26/26, security checks 25/25, and private Storage checks 21/21. Approval-audit transaction and local error-alert database checks passed with synthetic records that were rolled back or cleaned. Flutter analysis and 799 tests (1 skipped), plus `schooldesk-web` tests/typecheck/build, passed as recorded below. A regular debug APK was rebuilt against `env.local.json`; current wireless-device install/startup evidence is in [device verification](DEVICE_VERIFICATION.md). The local health response reports `storage=legacy-compatible`, while production reports `storage=r2-configured`; local storage checks do not exercise production R2.

**Release status remains BLOCKED.** FCM delivery/tap, background/reconnect, full app document upload/download and saved PDF rendering, remaining questionnaire cases, and iOS evidence are incomplete. Backend promotion is complete; it does not mark the mobile release ready.

## Tests executed

- Questionnaire structural verifier: 196 cases and 115 registered routes validated.
- Docker-local Supabase Edge Function tests: 26 passed, 0 failed.
- Docker-local API `/health` and `/ready`: HTTP 200.
- Local role/tenant security smoke: **25 checks passed after all four local reconciliation migrations**. Direct PostgREST checks confirmed School A visibility and School B exclusion for Principal, Teacher, and Parent.
- Approval Center API/database checks: synthetic leave and account approval mutations were returned with hydrated requester fields; leave dates matched the database. A Teacher leave → Principal approval → Teacher status handoff also passed, with the approving actor and one notification event/log verified in the database.
- `flutter analyze --no-pub`: no issues after configuring Very Good Analysis with an explicit baseline for pre-existing lint findings.
- Riverpod analyzer plug-in: opt-in config loaded successfully in an isolated analyzer check; the app currently uses `package:provider` and has no Riverpod usage.
- `dart run dependency_validator`: no dependency issues after removing unused direct dependencies `hotkey_manager`, `json_annotation`, and `url_launcher`.
- Full Flutter tests: 799 passed, 1 skipped. The suite was rerun after tool/dependency resolution and again passed 799 with 1 skip. Full coverage run also passed 799 with 1 skip; `coverage/lcov.info` reports 12,891/68,882 instrumented lines hit (18.7%). The local-only media URL test skipped by the full run was rerun with `env.local.json` and passed.
- Final full Flutter regression after the QA test-harness changes: **799 passed, 1 skipped, 0 failed**. Full `flutter analyze --no-pub`, questionnaire structural verification (196 cases / 115 routes), and `git diff --check` passed.
- Full Flutter regression rerun after the Settings Material surface and Android logout harness changes: **799 passed, 1 skipped, 0 failed**. Full analyzer passed with no issues; questionnaire verifier and diff check passed again.
- Approval Center unit/widget regression: 4 passed after formatting.
- Approval audit follow-up: the local transaction smoke passed under `postgres` and `service_role`; a missing reviewer was rejected with the decision rolled back and a Postgres log entry. Synthetic Principal GET `/audit-logs?module=approvals&user_id=current&page_size=10` returned HTTP 200 with zero prior rows. The migration was later promoted as `20260920171339` and the API as revision 277. Targeted Dart analysis passed; Docker-local Edge API tests passed **26/26** and Deno type-check passed for touched handlers. See [approval audit follow-up](reports/2026-09-20-approval-audit.md) and [promotion report](reports/2026-09-20-production-promotion.md).
- First-seen fatal/server API errors now create tenant-scoped Super Admin notification records and push events, with a five-per-school/five-minute alert cap; approval audit failures are explicitly escalated. The alert path was type-checked but not exercised on device or against live FCM. Failed error-report uploads are retained in memory and retried with capped backoff.
- `schooldesk-web` tests: 64 passed, 0 failed; 250 assertions. Typecheck passed. Production build passed with explicit local Docker/synthetic-school configuration.
- Web impact review: `schooldesk-web` is a Principal/Coordinator portal using the shared Edge API through `/api/backend/*`; its finance payment decision payload remains compatible. The current web portal has no Approval Center/audit-history screen or private student-document Storage flow, so these mobile/backend migrations need no web code change. Add a web Approval Center only if web parity is a product requirement.
- Web HTTP probes: synthetic Principal login returned 200, authenticated portal route returned 200, and a Teacher account sent to the Principal portal returned 403. This is HTTP evidence, not browser evidence.
- Moto G85 Android 16 integration: five local roles authenticated and reached expected dashboards; camera/gallery launch-cancel, synthetic PDF picker callback, share chooser, and print-preview handoffs also passed. Android notification permission was allowed. One Teacher logout/relogin flow later passed; other roles, FCM delivery/tap, app-form upload/download, reconnect, and saved/viewed PDF evidence remain incomplete. See the [device follow-up](reports/2026-09-20-android-device-followup.md) and [latest Android/storage follow-up](reports/2026-09-20-android-storage-followup.md).
- Moto G85 Android 16 logout/relogin follow-up: Teacher signed out through the portal drawer, secure session/role values cleared, and the same Teacher reached the dashboard after signing in again: **1 passed, 0 failed**. Other role logout flows remain unverified.
- Current local app handoff: a fresh regular debug APK was built with `--dart-define-from-file=env.local.json`, installed, and opened to the role-selection sign-in screen with the Docker API reverse forwarded; see [device verification](DEVICE_VERIFICATION.md). The phone is ready for the user's manual role walkthrough.
- Local private storage remediation smoke: **21 checks passed, 0 failed** after migration `20260920171431_20260920153106_restrict_private_files_to_service_api`. Principal and linked Parent API flows returned working signed URLs; direct object access/signing/upload/delete were denied for same-school and cross-school roles. The Parent's Storage list returned HTTP 200 without exposing the object. Cleanup verification found zero synthetic rows or blobs. Production now has zero `school_private_files_authenticated_*` policies. This tested the local Supabase Storage fallback, not production's R2 provider. Broader bucket, expiry, path traversal, retry, production R2, and deletion-lifecycle coverage remains incomplete; see [local private Storage remediation](reports/2026-09-20-local-private-storage-remediation.md) and [promotion report](reports/2026-09-20-production-promotion.md).

## Case tracker

`test_cases.csv` now records **166 Blocked**, **30 Not Run**, **0 Passed**, and **0 Failed** at the questionnaire-case level. The former local private-Storage failures (`API-011`, `RISK-002`, and `SHARED-013`) were retested after the local policy fix. Their focused access checks now pass; the composite cases remain Blocked for broader bucket/expiry/path/deletion coverage and live workflow evidence. See `QA-SEC-002` and `QA-SEC-003` in [known issues](known_issues.md).

Cases requiring manual Flutter interaction, a browser, a device, rendered/downloaded artifacts, live production evidence, or representative performance hardware are Blocked with the reason and this report linked. The remaining 30 cases are Not Run because their full API/RLS/static scope was not completed. They are not inferred Passed from source or regression tests.

## Backend verification

- Target: Docker-local project `schooldesk-local`; synthetic two-school fixtures only.
- Applied local schema through migration `20260920171431_20260920153106_restrict_private_files_to_service_api` (140 migrations total). Four reviewed migrations were also applied to production under the generated versions documented in the [promotion report](reports/2026-09-20-production-promotion.md).
- Edge API ran from the working tree containing the Approval Center changes.
- At the pre-promotion reconciliation checkpoint, 124 migration IDs were common, with 16 local-only IDs and 12 production-only IDs. On 2026-09-21, all 12 older timestamp aliases were mapped to production version IDs. Each environment and the checked-in chain now has 140 IDs with no version drift. Original hosted SQL bodies remain unavailable for some historical entries; see the [migration alignment report](reports/2026-09-21-migration-history-alignment.md), [earlier reconciliation snapshot](reports/2026-09-20-local-cloud-schema-reconciliation.md), and [promotion report](reports/2026-09-20-production-promotion.md).
- Both environments had 287 public policies with equivalent predicates at the reviewed checkpoint after normalizing production's scalar auth-helper wrappers. The forward migrations aligned helper definitions, removed redundant read-policy names and a duplicate notification-retention index, validated the fee-request status constraint, restored the legacy `pending` fee RPC guard, and restricted private-file Storage access. The four reviewed changes are now promoted.
- Docker catalog verification confirmed the three local auth helper hashes and search paths match production. A transaction-only helper smoke using synthetic Principal and Teacher JWT claims passed and rolled back cleanly.
- The private Storage policy remediation passed the focused 21-check Docker smoke. After promotion, a read-only production query confirmed zero `school_private_files_authenticated_*` policies on `school-private-files`; API/service-role access remains the intended path.
- The general local API security smoke initially received HTTP 429 during repeated fixture login attempts; after the rate-limit window cleared, a fresh post-migration run passed **25/25**.
- Historical note: the previous promotion gate was superseded on 2026-09-20. Backend promotion is complete; overall Android/mobile release remains blocked by the open checks above.

## Warnings and blockers

- The first security-smoke attempt received HTTP 502 where `/users` was expected to return 403. An immediate replay returned 403 and the complete second smoke run passed all 25 checks. The transient upstream response was not reproduced; see [known issues](known_issues.md).
- After the focused Storage run, one two-second `/health` probe timed out. Docker services remained up and PostgreSQL was ready; an immediate ten-second retry returned HTTP 200. This is recorded as a transient local probe, not a product failure.
- `schooldesk-web` first build attempt stopped on its required missing configuration. Rebuilding with explicit loopback API, synthetic school ID, and loopback site URL passed.
- No browser surface was available through CUA. The configured Pixel 2 AVD could not start because its Android 30 system image is missing. The Moto G85 wireless link recovered enough to complete five role login/dashboard integration cases; remaining native checks are still blocked. iOS tooling is unavailable on Linux.
- DCM 1.39.2 was removed because its CLI required an activation key. `very_good_analysis`, `riverpod_lint`, and `dependency_validator` are installed as project dev dependencies; see the follow-up report for versions and configuration.
- An initial Moto G85 APK install stalled, but later runs completed role and native plugin handoff checks. Full app upload/download, FCM, reconnect, logout/relogin, and artifact rendering remain open.
- The rebuilt APK now installs and opens the landing carousel on the Moto G85; role-based manual UI walkthroughs of approval history and actual FCM delivery have not been performed.
- Coverage is 18.7%; the suite passes, but many lines remain unexercised.
- Private Storage direct access is fixed in Docker and the four broad hosted policies were removed by the promoted migration. The previous Teacher direct-Storage delete failure is expected under API-only access; app-level document deletion and physical-object retention still need verification. These broader checks do not reverse the completed backend promotion.

**Regression status:** Automated/static, local API/RLS probes (25 role/tenant checks), web build gates, five Android role login/dashboard cases, one Android Teacher logout/relogin flow, camera/gallery cancel paths, synthetic PDF picker/share/print-preview handoffs, and the focused private Storage remediation smoke (21 checks) passed. The fresh regular debug APK also installed and displayed the landing carousel. Remaining Android workflow, browser, live production functional checks, saved/rendered PDF, app-level object deletion/retention, and live FCM evidence is incomplete. Tool installation and cloud history comparison are documented in [the tooling/cloud follow-up](reports/2026-09-20-tooling-device-cloud-followup.md); schema evidence is in [the local/cloud reconciliation](reports/2026-09-20-local-cloud-schema-reconciliation.md); Android outcomes are in the [device follow-up](reports/2026-09-20-android-device-followup.md) and [storage remediation](reports/2026-09-20-local-private-storage-remediation.md).

**Release status: BLOCKED.** Backend promotion occurred as described in the latest promotion update; overall release readiness remains blocked.
