org.gradle.jvmargs=-Djava.net.preferIPv4Stack=true -Djava.net.preferIPv6Addresses=false -Xmx1536m -XX:MaxMetaspaceSize=512m -XX:ReservedCodeCacheSize=256m -XX:+HeapDumpOnOutOfMemoryError
systemProp.java.net.preferIPv4Stack=true
systemProp.java.net.preferIPv6Addresses=false
# Dependency resolution must remain available for clean CI and when adding a
# package. Developers can still opt into offline builds with `--offline`.
org.gradle.offline=false
org.gradle.caching=true
org.gradle.parallel=true
org.gradle.daemon=true
# Disabled because Flutter 3.44.0's Gradle plugin captures Project/ConfigurationContainer
# objects in task actions that Gradle 9.5.1 cannot serialize to the configuration cache.
# Re-enable after upgrading to a Flutter/Gradle combination that supports it.
org.gradle.configuration-cache=false
android.useAndroidX=true
android.enableJetifier=false
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
